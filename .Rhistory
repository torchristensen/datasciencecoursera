filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
cc <- data.frame()
for(i in id)
{
nobs <- sum(complete.cases(read.csv(filesList[i])))
cc <- rbind(cc, c(i,nobs))
}
ccnames <- c("id","nobs")
colnames(cc) <- ccnames
cc
}
complete("specdata", c(6, 10, 20, 34, 100, 200, 310))
complete("specdata", 54)
set.seed(42)
cc <- complete("specdata", 332:1)
use <- sample(332, 10)
print(cc[use, "nobs"])
cr <- corr("specdata")
cr <- sort(cr)
set.seed(868)
out <- round(cr[sample(length(cr), 5)], 4)
print(out)
cr <- corr("specdata")
cr <- sort(cr)
set.seed(868)
out <- round(cr[sample(length(cr), 5)], 4)
print(out)
corr <- function(directory, threshold)
{
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
for(i in id)
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
if(complete.cases(monitorData.df[i]))
{
print(i)
}
}
}
corr("specdata")
corr("specdata",1)
corr <- function(directory, threshold)
{
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
for(i in filesList.length)
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
if(complete.cases(monitorData.df[i]))
{
print(i)
}
}
}
corr("specdata",1)
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
filesList <- list.files("specdata", pattern="*.csv", full.names=TRUE)
filesList.length
filesList.length()
length(filesList)
corr <- function(directory, threshold)
{
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
for(i in length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
if(complete.cases(monitorData.df[i]))
{
print(i)
}
}
}
corr("specdata",1)
corr <- function(directory, threshold)
{
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
for(i in length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
#monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
if(complete.cases(monitorData.df[i]))
{
print(i)
}
}
}
corr("specdata",1)
corr <- function(directory, threshold)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
for(i in length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
#monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
#if(complete.cases(monitorData.df[i]))
#{
# print(i)
#}
}
}
corr("specdata",1)
corr <- function(directory, threshold)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
for(i in length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
#monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
#if(complete.cases(monitorData.df[i]))
#{
# print(i)
#}
}
monitorData.df
}
corr("specdata",1)
corr <- function(directory, threshold)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
for(i in 1:length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
#monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
#if(complete.cases(monitorData.df[i]))
#{
# print(i)
#}
}
monitorData.df
}
corr("specdata",1)
corr <- function(directory, threshold)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
for(i in 1:length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
#monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
#if(complete.cases(monitorData.df[i]))
#{
# print(i)
#}
}
head(monitorData.df)
}
corr <- function(directory, threshold)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
for(i in 1:length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
#monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
#if(complete.cases(monitorData.df[i]))
#{
# print(i)
#}
}
head(monitorData.df)
}
corr("specdata",1)
corr <- function(directory, threshold)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
for(i in 1:length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
#monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
#if(complete.cases(monitorData.df[i]))
#{
# print(i)
#}
}
complete.cases(monitorData.df[1])
}
corr("specdata",1)
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
filesList <- list.files("specdata", pattern="*.csv", full.names=TRUE)
for(i in 1:length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))}
for(i in 1:length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
}
head(monitorData.df)
monitorData.df <- na.omit(monitorData.df)
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
for(i in 1:length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
monitorData.df <- na.omit(monitorData.df)
if (sum(complete.cases(monitorData.df)) >= threshold)
{
cr <- c(cr, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
#monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
#if(complete.cases(monitorData.df[i]))
#{
# print(i)
#}
monitorData.df
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
for(i in 1:length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
monitorData.df <- na.omit(monitorData.df)
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(cr, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
#monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
#if(complete.cases(monitorData.df[i]))
#{
# print(i)
#}
monitorData.df
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
for(i in 1:length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
monitorData.df <- na.omit(monitorData.df)
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
#monitorData.df <- rbind(monitorData.df,sum(complete.cases(monitorData.df[i])))
#if(complete.cases(monitorData.df[i]))
#{
# print(i)
#}
monitorData.df
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
for(i in 1:length(filesList))
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
monitorData.df <- na.omit(monitorData.df)
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
correlated
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
#for(i in 1:length(filesList))
for(i in 2)
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
monitorData.df <- na.omit(monitorData.df)
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
correlated
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
#for(i in 1:length(filesList))
for(i in 2)
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
length(monitorData.df)
monitorData.df <- na.omit(monitorData.df)
length(monitorData.df)
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
correlated
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
#for(i in 1:length(filesList))
for(i in 2)
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
print("full",length(monitorData.df))
monitorData.df <- na.omit(monitorData.df)
print("short",length(monitorData.df))
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
correlated
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
#for(i in 1:length(filesList))
for(i in 2)
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
print("full"&length(monitorData.df))
monitorData.df <- na.omit(monitorData.df)
print("short"&length(monitorData.df))
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
correlated
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
#for(i in 1:length(filesList))
for(i in 2)
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
print("full"+length(monitorData.df))
monitorData.df <- na.omit(monitorData.df)
print("short"+length(monitorData.df))
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
correlated
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
#for(i in 1:length(filesList))
for(i in 2)
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
paste("full",length(monitorData.df),sep=" ")
monitorData.df <- na.omit(monitorData.df)
paste("short",length(monitorData.df),sep=" ")
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
correlated
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
#for(i in 1:length(filesList))
for(i in 2)
{
monitorData.df <- rbind(monitorData.df,read.csv(filesList[i]))
paste(c("full",length(monitorData.df)),sep=" ")
monitorData.df <- na.omit(monitorData.df)
paste(c("short",length(monitorData.df)),sep=" ")
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
correlated
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
#for(i in 1:length(filesList))
for(i in 2)
{
monitorData.df <- read.csv(filesList[i])
paste(c("full",length(monitorData.df)),sep=" ")
monitorData.df <- na.omit(monitorData.df)
paste(c("short",length(monitorData.df)),sep=" ")
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
correlated
}
head(corr("specdata", 150))
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
for(i in 1:length(filesList))
{
monitorData.df <- read.csv(filesList[i])
paste(c("full",length(monitorData.df)),sep=" ")
monitorData.df <- na.omit(monitorData.df)
paste(c("short",length(monitorData.df)),sep=" ")
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
correlated
}
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
for(i in 1:length(filesList))
{
monitorData.df <- read.csv(filesList[i])
paste(c("full",length(monitorData.df)),sep=" ")
monitorData.df <- na.omit(monitorData.df)
paste(c("short",length(monitorData.df)),sep=" ")
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
paste(c("correlated:",correlated),sep = " ")
}
head(corr("specdata", 150))
cr <- corr("specdata")
cr <- sort(cr)
set.seed(868)
out <- round(cr[sample(length(cr), 5)], 4)
print(out)
corr <- function(directory, threshold = 0)
{
#Write a function that takes a directory of data files and a threshold for complete cases and calculates the correlation between sulfate and nitrate for monitor locations where the number of completely observed cases (on all variables) is greater than the threshold.
filesList <- list.files(directory, pattern="*.csv", full.names=TRUE)
correlated <- vector()
for(i in 1:length(filesList))
{
monitorData.df <- read.csv(filesList[i])
paste(c("full",length(monitorData.df)),sep=" ")
monitorData.df <- na.omit(monitorData.df)
paste(c("short",length(monitorData.df)),sep=" ")
if (sum(complete.cases(monitorData.df)) >= threshold)
{
correlated <- c(correlated, cor(x = monitorData.df$nitrate, y = monitorData.df$sulfate))
}
}
#paste(c("correlated:",correlated),sep = " ")
correlated
}
cr <- corr("specdata")
cr <- sort(cr)
set.seed(868)
out <- round(cr[sample(length(cr), 5)], 4)
print(out)
cr <- corr("specdata", 129)
cr <- sort(cr)
n <- length(cr)
set.seed(197)
out <- c(n, round(cr[sample(n, 5)], 4))
print(out)
cr <- corr("specdata", 2000)
n <- length(cr)
cr <- corr("specdata", 1000)
cr <- sort(cr)
print(c(n, round(cr, 4)))
my_matrix
